{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Supabase": {
    // Production/Default URL might go here if non-sensitive,
    // but often left blank or set via environment variables later.
    "Url": "",
    // Keys are sensitive - NEVER put real keys here.
    // Leave blank or use placeholders if you prefer.
    "Key": "",
    "ServiceKey": ""
  }
  // Add other non-sensitive configuration sections as needed
}